{
  "name": "@pipelinescore/mcp",
  "version": "0.1.0",
  "description": "MCP server for the PipelineScore LLM benchmark — expose run_benchmark / get_user_leaderboard / get_user_profile to any MCP-compatible client (Claude Code, Codex, Cursor, etc.).",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "pipelinescore-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
